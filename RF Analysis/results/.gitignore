# Large solver output stays local; the small result files (.s2p, summary.csv, solver logs, model.json, lines.json) are committed.
**/exc*/
**/*.kicad_pcb
**/farfield_*.json
# The repository root ignores *.log; solver logs are small and are the evidence for each run, so keep them.
!**/solver*.log
